<configuration>
    <!-- 控制台输出，含 MDC 中的 requestId / userId，便于本地调试与日志聚合。 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [req=%X{requestId:-} user=%X{userId:-}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 审计日志：登录、注销、删除、封禁、改密等敏感动作单独记录。 -->
    <logger name="bitmart.audit" level="INFO" additivity="true"/>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- 降低第三方库噪声。 -->
    <logger name="org.eclipse.jetty" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
</configuration>
