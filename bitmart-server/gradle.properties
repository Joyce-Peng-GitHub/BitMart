kotlin.code.style=official

# JVM args for Gradle daemon: enough heap for Kotlin compilation and tests.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Enable build/configuration cache for faster incremental builds.
org.gradle.caching=true
org.gradle.configuration-cache=true
